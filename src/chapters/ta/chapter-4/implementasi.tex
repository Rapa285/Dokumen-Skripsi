\section{Lingkungan Implementasi}
\label{sec:lingkungan-implementasi}

Implementasi sistem dilakukan pada lingkungan prototipe yang merepresentasikan
komponen utama DecMed, yaitu \textit{Patient Client}, \textit{Hospital Client},
\textit{Ministry Client}, PRE Server, penyimpanan \textit{off-chain} IPFS
Cluster, IOTA Gas Station, \textit{smart contract} IOTA Move, dan layanan
Audit Trail Service (ATS) yang berjalan pada jaringan IOTA Localnet. Lingkungan
ini dipilih agar rancangan pada Bab III dapat diuji tanpa bergantung pada
integrasi langsung dengan sistem informasi rumah sakit yang sesungguhnya.

Komponen ATS dan PRE Server dijalankan bersama pada lingkungan lokal bersama
ketiga aplikasi \textit{client}. Komponen pendukung yang membutuhkan ketersediaan
jaringan, yaitu IPFS Cluster dan IOTA Gas Station, dijalankan pada Virtual
Private Server (VPS). \textit{Smart contract} IOTA Move di-\textit{deploy} pada
jaringan IOTA Localnet yang juga berjalan di VPS. IOTA Localnet digunakan
karena sudah memadai untuk menyimulasikan interaksi dengan jaringan IOTA.

Spesifikasi perangkat lokal yang digunakan adalah sebagai berikut.
\begin{enumerate}
    \item Sistem Operasi: Ubuntu 24.04.4, berjalan di lingkungan WSL2.
    \item CPU: Intel Core i7-12700H (6 \textit{core} fisik dan 12 \textit{logical thread} dialokasikan untuk WSL2).
    \item Memori RAM: 32 GB (16 GB teralokasi untuk WSL2).
    \item Perangkat bantu pengembangan: Rust (\texttt{rustc 1.85.0},
    \texttt{cargo 1.85.0}), Node.js v22.14.0, dan Docker 27.4.0.
\end{enumerate}

Sementara itu, spesifikasi VPS yang digunakan adalah sebagai berikut.
\begin{enumerate}
    \item Sistem Operasi: Ubuntu 24.04.4 LTS.
    \item Jumlah vCPU: 4 vCPU.
    \item Memori RAM: 8 GB.
    \item Perangkat bantu: Docker 27.4.0.
\end{enumerate}

\section{Implementasi Sistem}

Bagian ini menjabarkan realisasi teknis dari rancangan pada Bab III. Narasi
konsep dan alur yang sudah ditetapkan di Bab III dirujuk ulang; fokus di sini
adalah komponen kode, antarmuka, dan perilaku konkret sistem.

Daftar komponen dan keterkaitannya dengan rancangan ditunjukkan pada
Tabel~\ref{tab:daftar-implementasi-ats}. ID I-ATS dipakai sebagai rujukan.
R-TA-01 (rancangan arsitektur solusi) mencakup keseluruhan sistem sehingga
tidak muncul sebagai baris tersendiri.

\begin{table}[htbp]
\centering
\caption{Daftar Implementasi Sistem Audit Trail}
\label{tab:daftar-implementasi-ats}
\renewcommand{\arraystretch}{1.3}
\begin{tabular}{|p{1.8cm}|p{5cm}|p{5.5cm}|}
\hline
\textbf{ID} & \textbf{Komponen} & \textbf{Merealisasikan Rancangan} \\
\hline
I-ATS-01 & Skema tipe data \texttt{AuditEvent}, \texttt{AuditRecord}, dan \texttt{AuditBatch}. & Katalog \textit{audit event} (EV1--EV13) dan skema atribut audit pada Subbab~\ref{subsec:rancangan-audit-record}. \\
\hline
I-ATS-02 & \textit{Endpoint} \texttt{POST /api/events} dan mekanisme verifikasi \textit{signed event}. & Subbab~\ref{subsec:rancangan-pembangkitan-event} (komponen Audit Receiver). \\
\hline
I-ATS-03 & Komponen \texttt{AuditLogger} dengan antrian asinkron internal. & Subbab~\ref{subsec:rancangan-pembentukan-record} (komponen Audit Logger dan mekanisme \textit{hash-chaining}). \\
\hline
I-ATS-04 & \textit{Worker} rotasi log, unggah IPFS, dan publikasi metadata ke IOTA. & Subbab~\ref{subsec:rancangan-rotasi-log}--\ref{subsec:rancangan-publikasi-iota} (Log Rotation \& Archival Worker). \\
\hline
I-ATS-05 & Modul \texttt{ats} dan \texttt{ATSClient} pada setiap \textit{event source}. & Subbab~\ref{subsec:rancangan-pembangkitan-event} (integrasi ATS ke dalam arsitektur DecMed). \\
\hline
\end{tabular}
\end{table}

Tabel~\ref{tab:daftar-implementasi-ats} menjadi acuan urutan pembahasan
implementasi berikut, dari skema tipe data, penerimaan \textit{event}, pembentukan
\textit{audit record} dengan \textit{hash-chaining}, rotasi log dan pengarsipan,
hingga integrasi ke seluruh \textit{event source} DecMed.

\subsection{Batasan Implementasi}

Implementasi mempertahankan arsitektur kerangka kerja DecMed sebagaimana
dijelaskan pada Subbab~\ref{subsec:rancangan-arsitektur}. Komponen perangkat
lunak ATS diimplementasikan sebagai \textit{crate} Rust yang berdiri sendiri.
Dependensi utama yang digunakan ditunjukkan pada Tabel~\ref{tab:dependensi-ats}.
Pada PRE Server yang telah ada, dependensi \texttt{ed25519-dalek} versi 2.x
ditambahkan untuk mendukung pembangkitan pasangan kunci penandatanganan
\textit{event} pada sisi pengirim. Batasan cakupan implementasi, konfigurasi
IPFS \textit{pinning}, dan nilai \texttt{LOG\_ROTATION\_INTERVAL\_SECS}
diuraikan pada Subbab~\ref{sec:pengujian}.

\begin{table}[htbp]
\centering
\caption{Dependensi Utama Audit Trail Service}
\label{tab:dependensi-ats}
\renewcommand{\arraystretch}{1.3}
\begin{tabular}{|p{3.5cm}|p{2cm}|p{6.5cm}|}
\hline
\textbf{Dependensi} & \textbf{Versi} & \textbf{Fungsi} \\
\hline
\texttt{axum} & 0.8.4 & \textit{Web framework} HTTP untuk \textit{endpoint} penerimaan \textit{audit event}. \\
\hline
\texttt{tokio} & 1.44.2 & \textit{Async runtime} Rust untuk pemrosesan konkuren. \\
\hline
\texttt{serde} / \texttt{serde\_json} & 1.x & Serialisasi dan deserialisasi data JSON. \\
\hline
\texttt{ed25519-dalek} & 3.0.0 & Verifikasi tanda tangan digital Ed25519 pada \textit{signed event}. \\
\hline
\texttt{sha2} & 0.10 & Komputasi \textit{hash} SHA-256 untuk mekanisme \textit{hash-chaining}. \\
\hline
\texttt{uuid} & 1.x & Pembangkitan \texttt{record\_id} berbasis UUID v7 yang bersifat monoton. \\
\hline
\texttt{chrono} & 0.4.39 & Penanganan waktu dan \textit{timestamp} dalam format UTC. \\
\hline
\texttt{iota-sdk} & 1.2.0-alpha & Interaksi dengan jaringan IOTA untuk publikasi metadata rotasi log. \\
\hline
\texttt{reqwest} & 0.12 & Klien HTTP untuk unggah berkas ke IPFS dan interaksi dengan Gas Station. \\
\hline
\texttt{tokio-util} & 0.7.11 & \textit{Streaming} berkas saat unggah ke IPFS menggunakan \texttt{BytesCodec}. \\
\hline
\end{tabular}
\end{table}

\subsection{Implementasi Skema Tipe Data Audit}
\label{subsec:impl-skema-tipe-data}

Implementasi skema tipe data audit merealisasikan katalog \textit{audit event} dan skema atribut yang telah ditentukan pada Subbab~\ref{subsec:rancangan-audit-record}. Seluruh tipe data didefinisikan pada berkas \texttt{audit-trail/src/types.rs} dan terdiri atas lima tipe utama: \texttt{SignedEvent}, \texttt{AuditEvent}, \texttt{AuditEventDetails}, \texttt{AuditRecord}, dan \texttt{AuditBatch}.

\subsubsection{Tipe Data SignedEvent}

\texttt{SignedEvent} adalah struktur yang diterima dari komponen pengirim melalui \textit{endpoint} HTTP. Tipe ini memuat tiga bidang: \texttt{payload} sebagai representasi JSON dari \textit{audit event}, \texttt{signature} sebagai tanda tangan digital Ed25519 atas \texttt{payload} dalam representasi heksadesimal (64 byte), dan \texttt{public\_key} sebagai kunci publik pengirim dalam representasi heksadesimal (32 byte). Implementasi \texttt{SignedEvent} ditunjukkan pada Kode~\ref{code:signed-event}.

\begin{listing}[ht]
\caption{Implementasi \texttt{SignedEvent}}
\label{code:signed-event}

\begin{minted}[
    linenos,
    breaklines
]{rust}
#[derive(Debug, Deserialize)]
pub struct SignedEvent {
    pub payload: String,
    pub signature: String,
    pub public_key: String,
}
\end{minted}
\end{listing}

\subsubsection{Tipe Data AuditEvent}

\texttt{AuditEvent} mewakili satu peristiwa audit yang telah diekstrak dari \texttt{SignedEvent} dan terverifikasi. Tipe ini memuat lima atribut umum yang wajib ada pada seluruh jenis \textit{event} sesuai Tabel~\ref{tab:atribut-umum-event}: \texttt{source\_component} (komponen sumber pembangkit \textit{event}), \texttt{actor\_id} (identitas aktor yang memicu aktivitas), \texttt{target\_object} (identitas objek yang menjadi target aktivitas), \texttt{outcome} (hasil aktivitas bertipe \texttt{AuditOutcome}), dan \texttt{action\_type} (jenis tindakan yang dilakukan). Atribut tambahan yang spesifik terhadap jenis \textit{event} dimuat melalui bidang \texttt{details} bertipe \texttt{AuditEventDetails} yang diserialisasi sejajar menggunakan anotasi \texttt{\#[serde(flatten)]}. Implementasinya ditunjukkan pada Kode~\ref{code:audit-event}.

\begin{minted}{rust}
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct AuditEvent {
    pub source_component: String,
    pub actor_id: String,
    pub target_object: String,
    pub outcome: AuditOutcome,
    pub action_type: String,

    #[serde(flatten)]
    pub details: AuditEventDetails,
}
\end{minted}

\captionof{listing}{Implementasi \texttt{AuditEvent}}
\label{code:audit-event}

\texttt{AuditOutcome} diimplementasikan sebagai \textit{enum} dengan empat varian: \texttt{Success}, \texttt{Failure}, \texttt{Denied}, dan \texttt{Unknown}, merealisasikan atribut \texttt{outcome} pada skema atribut umum.

\subsubsection{Tipe Data AuditEventDetails}

\texttt{AuditEventDetails} diimplementasikan sebagai \textit{tagged enum} dengan anotasi \texttt{\#[serde(tag = "event\_type")]} sehingga nilai \texttt{event\_type} pada JSON yang masuk menentukan varian \textit{enum} yang akan di-\textit{parse}. Terdapat 13 varian yang masing-masing merealisasikan satu jenis \textit{audit event} (EV1--EV13) sesuai Tabel~\ref{tab:threat-event-mapping}. Atribut tambahan pada setiap varian mengikuti Tabel~\ref{tab:detail-audit-record}; perbedaan antarjenis \textit{event} terletak seluruhnya pada bidang dalam varian masing-masing. Sebagai contoh, varian \texttt{EV7} untuk \textit{event} permintaan layanan PRE memuat atribut \texttt{endpoint\_called}, \texttt{request\_id}, \texttt{caller\_component}, dan \texttt{channel\_encryption}, sedangkan varian \texttt{EV13} untuk validasi \textit{capability} memuat \texttt{capability\_id}, \texttt{requester\_id}, \texttt{validation\_result} (bertipe \texttt{bool}), dan \texttt{rejection\_reason} (bertipe \texttt{Option<String>} yang bernilai \texttt{None} apabila validasi berhasil). Implementasi seluruh varian \texttt{AuditEventDetails} ditunjukkan pada Kode~\ref{code:audit-event-details}.

\subsubsection{Tipe Data AuditRecord}

\texttt{AuditRecord} mewakili catatan audit yang telah terbentuk dan siap disimpan ke berkas log. Tipe ini memuat empat bidang tambahan di luar konten \textit{event}: \texttt{record\_id} bertipe \texttt{Uuid} (UUID v7 yang mengandung informasi waktu sehingga bersifat monoton dan dapat diurutkan secara kronologis), \texttt{timestamp} bertipe \texttt{DateTime<Utc>} (waktu pembentukan \textit{record}), \texttt{prev\_record\_hash} bertipe \texttt{Option<String>} (nilai \textit{hash} SHA-256 dari \textit{record} sebelumnya, atau \texttt{None} apabila merupakan \textit{record} pertama dalam rantai), dan \texttt{record\_hash} bertipe \texttt{String} (nilai \textit{hash} SHA-256 yang dihitung dari kombinasi keempat bidang tersebut bersama isi \textit{event}). Seluruh atribut \texttt{AuditEvent} diserialisasi sejajar ke dalam \texttt{AuditRecord} menggunakan \texttt{\#[serde(flatten)]}, sehingga berkas JSON-Lines yang dihasilkan memuat seluruh atribut pada satu tingkat yang sama. Implementasi \texttt{AuditRecord} ditunjukkan pada Kode~\ref{code:audit-record}.

\begin{minted}{rust}
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct AuditRecord {
    pub record_id: Uuid,
    pub timestamp: DateTime<Utc>,
    pub prev_record_hash: Option<String>,
    pub record_hash: String,

    #[serde(flatten)]
    pub event: AuditEvent,
}
\end{minted}

\captionof{listing}{Implementasi \texttt{AuditRecord}}
\label{code:audit-record}

\subsubsection{Tipe Data AuditBatch}

\texttt{AuditBatch} merepresentasikan ringkasan satu berkas log yang telah dirotasi dan diarsipkan ke IPFS. Tipe ini tidak disimpan dalam berkas log itu sendiri, melainkan dibentuk oleh \textit{worker} setelah proses rotasi selesai sebagai metadata yang merangkum isi berkas arsip. Bidang-bidang pada \texttt{AuditBatch} mencakup identitas dan rentang waktu batch (\texttt{batch\_id}, \texttt{start\_time}, \texttt{end\_time}), ringkasan isi (\texttt{record\_count}, \texttt{first\_record\_id}, \texttt{last\_record\_id}), nilai \textit{hash} rekam pertama dan terakhir untuk verifikasi ujung rantai (\texttt{first\_record\_hash}, \texttt{final\_record\_hash}), serta informasi persistensi (\texttt{file\_hash} sebagai \textit{hash} SHA-256 berkas arsip secara keseluruhan, dan \texttt{ipfs\_cid} sebagai CID yang dihasilkan setelah unggah berhasil). Implementasi \texttt{AuditBatch} ditunjukkan pada Kode~\ref{code:audit-batch}.

\begin{minted}{rust}
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct AuditBatch {
    pub batch_id: Uuid,
    pub start_time: DateTime<Utc>,
    pub end_time: DateTime<Utc>,
    pub record_count: u64,
    pub first_record_id: Uuid,
    pub last_record_id: Uuid,
    pub first_record_hash: String,
    pub final_record_hash: String,
    pub file_hash: String,
    pub ipfs_cid: String,
}
\end{minted}

\captionof{listing}{Implementasi \texttt{AuditBatch}}
\label{code:audit-batch}

text

\subsection{Implementasi Audit Receiver}

Komponen Audit Receiver diimplementasikan melalui \textit{handler}
\texttt{handle\_event} pada berkas \texttt{audit-trail/src/handlers.rs} dan
\textit{routing} pada \texttt{audit-trail/src/main.rs}. \textit{Endpoint}
\texttt{POST /api/events} menerima \textit{request} berisi objek
\texttt{SignedEvent} dalam format JSON.

Proses pada \textit{handler} \texttt{handle\_event} terdiri atas dua fase.
Fase pertama adalah verifikasi sumber \textit{event} melalui fungsi
\texttt{Utils::verify\_and\_extract\_event}. Fungsi ini melakukan tiga langkah:
mendekode kunci publik dari representasi heksadesimal menjadi
\texttt{VerifyingKey} Ed25519, mendekode tanda tangan menjadi \texttt{Signature}
Ed25519, kemudian memverifikasi tanda tangan terhadap \textit{byte-byte payload}.
Apabila verifikasi berhasil, \textit{payload string} diurai menjadi objek
\texttt{AuditEvent}. Apabila verifikasi gagal, \textit{handler} mengembalikan
respons \textit{error} tanpa memproses \textit{event} lebih lanjut.

Fase kedua adalah memasukkan \textit{event} yang telah terverifikasi ke dalam
antrian asinkron internal melalui \texttt{state.audit\_tx.send(audit\_event).await}.
Antrian diimplementasikan menggunakan \texttt{tokio::sync::mpsc} dengan kapasitas
10.000 \textit{event}. Desain ini memisahkan proses penerimaan \textit{event}
dari proses pembentukan dan penyimpanan \textit{audit record}, sehingga keduanya
dapat berjalan secara konkuren tanpa saling memblokir.

Status implementasi komponen Audit Receiver ditunjukkan pada
Tabel~\ref{tab:status-implementasi-receiver}.

\begin{table}[htbp]
\centering
\caption{Status Implementasi Komponen Audit Receiver}
\label{tab:status-implementasi-receiver}
\renewcommand{\arraystretch}{1.3}
\begin{tabular}{|p{8cm}|p{3.5cm}|}
\hline
\textbf{Bagian} & \textbf{Status} \\
\hline
\textit{Endpoint} \texttt{POST /api/events} & Terimplementasi. \\
\hline
Dekode kunci publik dan tanda tangan Ed25519 & Terimplementasi. \\
\hline
Verifikasi tanda tangan terhadap \textit{payload} & Terimplementasi. \\
\hline
\textit{Parsing} \texttt{AuditEvent} dari \textit{payload} yang valid & Terimplementasi. \\
\hline
Pengiriman ke antrian asinkron internal (kapasitas 10.000) & Terimplementasi. \\
\hline
Penolakan \textit{event} dengan tanda tangan tidak valid & Terimplementasi. \\
\hline
\end{tabular}
\end{table}

\subsection{Implementasi Audit Logger}

Komponen Audit Logger diimplementasikan sebagai \texttt{struct AuditLogger}
pada berkas \texttt{audit-trail/src/audit.rs}. Komponen ini berjalan sebagai
\textit{task} Tokio terpisah yang di-\textit{spawn} pada saat aplikasi dimulai.

\texttt{AuditLogger} menyimpan dua bidang: \texttt{rx} bertipe
\texttt{Receiver<AuditEvent>} sebagai sisi penerima antrian asinkron, dan
\texttt{prev\_record\_hash} bertipe \texttt{Option<String>} sebagai \textit{hash}
dari \textit{record} sebelumnya untuk keperluan \textit{hash-chaining}.

Metode \texttt{run} menjalankan \textit{loop} utama menggunakan pola
\texttt{while let Some(event) = self.rx.recv().await}. Untuk setiap \textit{event},
\textit{loop} melakukan tiga langkah berurutan: (1) memanggil fungsi
\texttt{create\_audit\_record} untuk membentuk \texttt{AuditRecord} baru,
(2) memanggil \texttt{write\_audit\_record} untuk menulis \textit{record} ke
berkas log, dan (3) memperbarui \texttt{self.prev\_record\_hash} dengan
\textit{hash} dari \textit{record} yang baru saja ditulis.

Fungsi \texttt{create\_audit\_record} merealisasikan mekanisme
\textit{hash-chaining} dengan membangkitkan \texttt{record\_id} baru menggunakan
\texttt{Uuid::now\_v7()}, mengambil waktu saat ini menggunakan \texttt{Utc::now()},
kemudian mendelegasikan komputasi \textit{hash} ke
\texttt{Utils::calculate\_record\_hash}. Fungsi \texttt{calculate\_record\_hash}
menggabungkan \texttt{record\_id}, \texttt{timestamp}, \texttt{prev\_record\_hash},
dan isi \textit{event} menjadi satu objek JSON, kemudian menghitung SHA-256 dari
representasi JSON tersebut dan mengembalikannya sebagai \textit{string} heksadesimal.

Fungsi \texttt{write\_audit\_record} membuka berkas log dengan mode
\textit{append} menggunakan \texttt{OpenOptions::new().create(true).append(true)},
kemudian menulis representasi JSON dari \textit{record} diikuti karakter baris baru.
Penggunaan mode \textit{append} menjamin bahwa entri yang sudah ada tidak pernah
tertimpa, merealisasikan karakteristik \textit{append-only} yang disyaratkan oleh
KNF-TA-01. Konstanta \texttt{LOG\_FILE\_PATH} yang merujuk ke
\texttt{./logs/audit\_logs.log} didefinisikan pada \texttt{audit-trail/src/constants.rs}.

\subsection{Implementasi Log Rotation dan Archival Worker}

Komponen Log Rotation \& Archival Worker diimplementasikan sebagai fungsi
\texttt{Utils::spawn\_log\_rotation\_worker} pada berkas
\texttt{audit-trail/src/utils.rs}. Fungsi ini men-\textit{spawn} sebuah
\textit{task} Tokio yang berjalan selamanya dengan interval yang dikonfigurasi
melalui konstanta \texttt{LOG\_ROTATION\_INTERVAL\_SECS}.

Setiap siklus rotasi, \textit{worker} melakukan tiga tahap berurutan.

\textbf{Tahap 1 --- Rotasi berkas log.} \textit{Worker} memeriksa apakah
berkas log aktif ada dan tidak kosong. Apabila berkas kosong, siklus dilewati
untuk menghindari pencatatan metadata rotasi yang tidak bermakna pada jaringan
IOTA. Apabila berkas berisi data, \textit{worker} membentuk nama berkas arsip
baru dengan format \texttt{logs/uploading\_\{unix\_timestamp\}.log} lalu
memindahkan berkas log aktif ke nama arsip baru menggunakan \texttt{fs::rename}.
Operasi \texttt{rename} bersifat atomik pada sistem berkas Linux sehingga tidak
terjadi kehilangan \textit{record} yang sedang ditulis secara konkuren oleh Audit
Logger. Awalan \texttt{uploading\_} berfungsi sebagai penanda bahwa berkas sedang
dalam proses pengarsipan, sehingga dapat diidentifikasi dan diproses ulang pada
siklus berikutnya apabila terjadi kegagalan.

\textbf{Tahap 2 --- Unggah ke IPFS.} \textit{Worker} menghitung nilai
\textit{hash} SHA-256 dari berkas arsip menggunakan \texttt{IotaLogClient::hash\_file},
kemudian mengunggah berkas ke IPFS Cluster melalui fungsi
\texttt{Utils::add\_file\_to\_ipfs}. Fungsi ini menggunakan \texttt{FramedRead}
dengan \texttt{BytesCodec} untuk melakukan \textit{streaming} berkas tanpa memuat
seluruh konten ke memori sekaligus. Respons dari IPFS menghasilkan CID yang
menjadi penanda konten berkas. Apabila unggah gagal, siklus dihentikan dan
berkas arsip tetap berada di penyimpanan lokal dengan awalan \texttt{uploading\_}
untuk diproses ulang pada siklus berikutnya.

\textbf{Tahap 3 --- Publikasi metadata ke IOTA.} \textit{Worker} membentuk
objek \texttt{IotaLogMetadata} yang memuat lima bidang sesuai skema pada
Tabel~\ref{tab:skema-metadata-iota}: \texttt{cid}, \texttt{file\_hash},
\texttt{log\_sequence\_number}, \texttt{rotation\_time}, dan
\texttt{prev\_tx\_digest}. Bidang \texttt{prev\_tx\_digest} membentuk rantai
antar-entri metadata \textit{on-chain}, analog dengan mekanisme
\textit{hash-chaining} pada level \textit{audit record}. Metadata diterbitkan
ke jaringan IOTA melalui \texttt{IotaLogClient::publish\_metadata}.

Implementasi \texttt{IotaLogClient} pada berkas
\texttt{audit-trail/src/iota\_client.rs} menangani seluruh interaksi dengan
jaringan IOTA. Fungsi \texttt{publish\_metadata} menserialisasi metadata ke JSON
\textit{string}, membangun transaksi IOTA menggunakan
\texttt{ProgrammableTransactionBuilder}, mereservasi \textit{gas} dari Gas Station
Server, menandatangani transaksi dengan kunci privat ATS, kemudian mengeksekusi
transaksi melalui Gas Station Server. Apabila transaksi berhasil, fungsi
mengembalikan \texttt{PublishResult} yang memuat \texttt{object\_id} objek
\texttt{LogRecord} yang dibuat \textit{on-chain} serta \texttt{tx\_digest}
transaksi.

Setelah publikasi ke IOTA berhasil, \textit{worker} memperbarui
\texttt{prev\_tx\_digest} untuk siklus berikutnya, kemudian menghapus berkas
arsip dari penyimpanan lokal menggunakan \texttt{fs::remove\_file}. Retensi
jangka panjang bergantung pada mekanisme \textit{pinning} IPFS Cluster yang sama
yang telah digunakan DecMed untuk data klinis pasien.

Status implementasi \textit{worker} ditunjukkan pada
Tabel~\ref{tab:status-implementasi-worker}.

\begin{table}[htbp]
\centering
\caption{Status Implementasi Log Rotation dan Archival Worker}
\label{tab:status-implementasi-worker}
\renewcommand{\arraystretch}{1.3}
\begin{tabular}{|p{8cm}|p{3.5cm}|}
\hline
\textbf{Bagian} & \textbf{Status} \\
\hline
Rotasi berkas log aktif dengan operasi atomik \texttt{rename} & Terimplementasi. \\
\hline
Perhitungan \textit{hash} SHA-256 berkas arsip sebelum unggah & Terimplementasi. \\
\hline
Unggah berkas arsip ke IPFS melalui \textit{streaming} \texttt{BytesCodec} & Terimplementasi. \\
\hline
\textit{Pinning} berkas arsip pada IPFS Cluster & Terimplementasi. \\
\hline
Pembentukan dan publikasi \texttt{IotaLogMetadata} ke IOTA melalui Gas Station & Terimplementasi. \\
\hline
Pembentukan rantai \texttt{prev\_tx\_digest} antar-\texttt{LogRecord} & Terimplementasi. \\
\hline
Penghapusan berkas arsip lokal setelah publikasi berhasil & Terimplementasi. \\
\hline
Penanganan kegagalan siklus dengan retensi berkas \texttt{uploading\_*} & Terimplementasi. \\
\hline
\end{tabular}
\end{table}

\subsection{Implementasi Smart Contract IOTA Move}

\textit{Smart contract} IOTA Move yang digunakan ATS didefinisikan pada
\texttt{move/ats/sources/audit\_trail.move}. Modul \texttt{ats::audit\_log}
menyediakan satu fungsi utama, yaitu \texttt{create\_log}, yang menerima
representasi JSON dari metadata rotasi dan membuat objek \texttt{LogRecord}
baru pada \textit{ledger} IOTA.

Aspek terpenting dari rancangan \textit{smart contract} ini adalah bahwa modul
\texttt{ats::audit\_log} tidak menyediakan fungsi apa pun untuk mengubah atau
menghapus objek \texttt{LogRecord} yang telah dibuat. Satu-satunya operasi yang
tersedia adalah \texttt{create\_log}. Setiap objek \texttt{LogRecord} yang dibuat
langsung dibekukan melalui \texttt{transfer::freeze\_object}, sehingga tidak ada
entitas mana pun --- termasuk ATS itu sendiri --- yang dapat mengubah isinya
setelah transaksi pembuatannya \textit{final}. Rancangan ini secara langsung
mengatasi masalah M-TA-03 yang mengidentifikasi bahwa mekanisme pencatatan
eksisting pada DecMed masih menyediakan fungsi pembaruan pada \textit{smart
contract} Move yang ada.

\subsection{Implementasi Integrasi ATS ke DecMed}

Integrasi ATS ke dalam arsitektur DecMed dilakukan secara seragam pada seluruh
\textit{event source} yang telah diidentifikasi: PRE Server, \textit{Patient Client},
\textit{Hospital Client}, dan \textit{Ministry Client}. Setiap \textit{event source}
diberikan penambahan modul \texttt{ats} dengan pola struktur yang konsisten di
setiap komponen: berkas \texttt{ats.rs} yang memuat implementasi utama dan
\texttt{mod.rs} yang mengekspor tipe-tipe publik.

\subsubsection{Tipe Data Bersama}

Pada setiap \textit{event source}, berkas \texttt{ats.rs} mendefinisikan ulang
tipe-tipe data \texttt{AuditEvent}, \texttt{AuditOutcome}, dan
\texttt{AuditEventDetails} yang strukturnya setara dengan yang ada di ATS.
Pendekatan ini dipilih agar setiap komponen sumber \textit{event} tidak bergantung
pada \textit{crate} ATS secara langsung, mempertahankan independensi
antarkomponen. Varian \texttt{AuditEventDetails} yang diimplementasikan pada
masing-masing \textit{event source} dibatasi hanya pada jenis \textit{event} yang
relevan dengan aktivitas komponen tersebut.

\subsubsection{Implementasi ATSClient}

Setiap modul \texttt{ats} menyediakan \textit{struct} \texttt{ATSClient} yang
mengelola pasangan kunci Ed25519 untuk penandatanganan \textit{event} serta
mengirimkan \textit{signed event} ke ATS. Kunci Ed25519 dibangkitkan secara acak
pada saat inisialisasi menggunakan \texttt{SigningKey::generate(\&mut OsRng)},
sehingga setiap instans komponen memiliki kunci penandatanganan yang unik. Kunci
publik yang bersesuaian disertakan pada setiap \textit{signed event} agar ATS
dapat memverifikasi tanda tangan tanpa perlu menyimpan daftar kunci yang diizinkan
terlebih dahulu.

\texttt{ATSClient} menyediakan dua metode pengiriman. Metode \texttt{send\_event}
bersifat \textit{async} dan menunggu respons dari ATS. Metode
\texttt{send\_event\_nonblocking} men-\textit{spawn task} Tokio terpisah yang
berjalan secara \textit{fire-and-forget}; apabila pengiriman gagal, \textit{error}
hanya dicatat ke \textit{stderr} tanpa memblokir alur utama. Metode kedua
digunakan pada seluruh instrumentasi di setiap \textit{event source} agar operasi
audit tidak menambah latensi pada alur layanan utama.

\subsubsection{Integrasi ke State Komponen}

Pada setiap \textit{event source}, sebuah bidang \texttt{ats\_client: ATSClient}
ditambahkan pada \textit{struct state} utama komponen tersebut --- misalnya
\texttt{AppState} pada PRE Server. Inisialisasi dilakukan pada titik masuk masing-masing
komponen dengan membaca \texttt{ATS\_ENDPOINT} dari \textit{environment variable}.
Nilai \textit{default endpoint} adalah \texttt{http://localhost:3000/api/events}
apabila variabel tidak dikonfigurasi.

\subsubsection{Distribusi Event per Event Source}

\textit{Patient Client}, \textit{Hospital Client}, dan \textit{Ministry Client}
merupakan aplikasi \textit{client} berbasis Tauri. Pada ketiga aplikasi tersebut,
modul \texttt{ats} diimplementasikan pada sisi \textit{backend} Rust dari aplikasi
Tauri, sehingga penandatanganan dan pengiriman \textit{event} tidak terekspos ke
lapisan antarmuka SvelteKit/TypeScript. Distribusi \textit{event} berdasarkan
\textit{event source} ditunjukkan pada Tabel~\ref{tab:pemetaan-event-source}.

\begin{table}[htbp]
\centering
\caption{Distribusi \textit{Audit Event} per \textit{Event Source}}
\label{tab:pemetaan-event-source}
\renewcommand{\arraystretch}{1.3}
\begin{tabular}{|p{2.8cm}|p{1.8cm}|p{7.5cm}|}
\hline
\textbf{\textit{Event Source}} & \textbf{Jumlah \textit{Event}} & \textbf{Kode \textit{Audit Event}} \\
\hline
PRE Server & 9 & EV7, EV8, EV9, EV10, EV11, EV12, EV13 \\
\hline
\textit{Patient Client} & 6 & EV1, EV2, EV3, EV8, EV9, EV10 \\
\hline
\textit{Hospital Client} & 5 & EV1, EV4, EV8, EV9, EV10 \\
\hline
\textit{Ministry Client} & 4 & EV5, EV6, EV8, EV9 \\
\hline
\end{tabular}
\end{table}

Perlu dicatat bahwa \textit{event} EV9 (Gas Sponsorship Request) dan EV8 (IOTA
Transaction Submission) tercatat pada sisi pemohon, bukan pada Gas Station Server.
Gas Station Server diperlakukan sebagai infrastruktur pendukung yang tidak
diinstrumentasi sebagai \textit{event source} tersendiri; keputusan \textit{sponsorship}
yang dihasilkannya cukup direkam oleh komponen pemanggil.

\subsection{Implementasi Verifikasi Integritas Audit Record}

Fungsi \texttt{Utils::verify\_record} pada berkas \texttt{audit-trail/src/utils.rs} merealisasikan mekanisme verifikasi integritas satu \textit{audit record}. Fungsi ini menghitung ulang \texttt{record\_hash} dari \texttt{record\_id}, \texttt{timestamp}, \texttt{prev\_record\_hash}, dan isi \textit{event} menggunakan \texttt{calculate\_record\_hash}, kemudian membandingkannya dengan \texttt{record\_hash} yang tersimpan. Kesamaan nilai keduanya membuktikan bahwa \textit{record} tidak mengalami modifikasi sejak dibuat.

Verifikasi rantai secara penuh dilakukan dengan memeriksa setiap \textit{record} secara berurutan: nilai \texttt{prev\_record\_hash} setiap \textit{record} harus sama persis dengan \texttt{record\_hash} dari \textit{record} sebelumnya. Apabila terdapat \textit{record} yang dihapus, ditambahkan, atau dimodifikasi di tengah rantai, nilai \texttt{record\_hash} seluruh \textit{record} sesudahnya akan menjadi tidak konsisten dan verifikasi akan gagal.